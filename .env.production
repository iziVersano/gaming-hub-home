# Production API Configuration
#
# Same-origin Vercel serverless functions (api/). The previous value pointed at
# https://consoltech-api.onrender.com/api, which has been suspended and returns
# 503 — every admin screen hung on it.
#
# Implemented today: /api/auth/login, /api/warranty, /api/products.
# NOT implemented: /api/places, /api/upload — these now 404 rather than 503.
VITE_API_URL=/api
